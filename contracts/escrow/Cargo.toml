[package]
name = "escrow"
version = "0.1.0"
edition = "2021"
description = "AgentPay escrow and settlement contract"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "22.0"

[dev-dependencies]
soroban-sdk = { version = "22.0", features = ["testutils"] }
